{
  "name": "pi-plugins",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "check-types": "turbo check-types",
    "test": "turbo test",
    "format": "turbo format",
    "format:check": "turbo format:check"
  },
  "devDependencies": {
    "turbo": "2.8.11"
  }
}
