---
'@pi-plugins/usage': patch
---

Group provider sections by severity into a single info and a single warning message, since the UI only shows one message per severity and all-success runs previously displayed only the last provider
