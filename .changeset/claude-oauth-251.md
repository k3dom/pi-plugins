---
'@pi-plugins/claude-oauth': patch
---

Update Anthropic OAuth requests to match Claude Code 2.1.251's local-agent headers, identity, metadata, payload fields, attribution, and canonical request attestation while leaving API-key requests untouched.
