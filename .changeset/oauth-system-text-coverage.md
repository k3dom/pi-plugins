---
'@pi-plugins/claude-oauth': patch
---

Remove pi identity from the `<docs>`-wrapped documentation section and from the mid-conversation `role: "system"` messages Pi 0.86 sends to newer Claude models.
