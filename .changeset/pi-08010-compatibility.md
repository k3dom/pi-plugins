---
'@pi-plugins/fast-mode': patch
'@pi-plugins/usage': patch
---

Restore Pi 0.80.10 compatibility by using the current model-registry credential APIs
and removing the retired bare GPT-5.6 alias from fast-mode defaults.
